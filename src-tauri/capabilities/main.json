{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Totonoe の署名済みローカル UI に必要な最小権限",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "application-commands"
  ]
}

