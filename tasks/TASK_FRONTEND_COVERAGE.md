# タスク: 画面側の検証を、名ばかりで終わらせない

`BRIEF.md` と `docs/RULES.md` を読め。**Rust には触るな。`src/` だけ。**

## いまの状態

`src/frontendLogic.ts` に純関数が切り出され、`src/frontendLogic.test.ts` に11件のテストがある。
`npm run build` が走らせる。ここまでは出来ている。

**足りないのは範囲。** `src/App.tsx` は801行あり、切り出されているのはごく一部。

## やること

1. **まず、いまのテストが本物か確かめろ。**
   `src/frontendLogic.ts` の各関数について、本体を定数や恒真式に置き換えたとき、
   **どのテストが落ちるか**を調べろ。**1件も落ちない関数**があれば、それは守られていない。
   結果を表にして報告しろ。

2. **守られていない関数にテストを足せ。**

3. `src/App.tsx` と `src/components/` に残っている**判断の論理**のうち、
   純関数へ切り出せるものを切り出してテストしろ。特に:
   - どの操作が live でないと出来ないかの判定
   - エラー状態の遷移（出す・消す・上書きする）
   - 一覧の絞り込みと選択の関係
   - 直前適用（`justApplied`）の増減

   **切り出しは最小限に。** 画面の構造を作り替えるな。

## 絶対に守ること

- **見た目を変えるな。** 論理を移すだけ
- テストのために本番の挙動を変えるな
- `npm run build`（typecheck と test を含む）が通ること

## 検証

**「テストを足した」を成果にするな。**
足した各テストについて、**それが捕まえる欠陥を注入したら落ちること**を確認して報告しろ。
落ちないテストは消せ。

## 完了条件

- `npm run build` が通る（CSS構文警告0件、テスト全通過）
- `npm run typecheck` が通る
- `cargo test --lib` が通る（405件のまま）
- 手順1の表を報告に含める
