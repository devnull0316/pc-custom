# 変更履歴

## 0.1.0（未リリース）

最初の公開版。

### できること

- 登録 Action 75 件。うち変更できるのは 20 件、状態の確認だけが 9 件、
  表示専用が 39 件、Windows の設定画面へ案内するものが 6 件。
  表示専用が多い理由は [README](README.md) に書いている。
- 適用した変更はタイムラインに 1 件ずつ残り、それぞれ独立して元へ戻せる。
- 利用場面ごとのモード。手動起動と、登録したアプリの起動・終了に連動。
- 新しい PC のセットアップ支援。WinGet 経由で固定 12 アプリの導入。
- ウィンドウ配置の保存と復元。ゲームのウィンドウは対象外。
- 場面名とインストール済みプリンターを明示的に選び、今回だけ既定にして終了時に
  直前の既定へ戻す。Windows の自動管理が有効なら変更せず、印刷は行わない。
- Windowsに登録済みのVPNを明示的に選び、認証情報を受け取らず今回だけ接続する。
  開始前から接続中なら何もせず、同じ実行中に自分が得た接続ハンドルだけを切断する。
  再起動後は所有を推測せず、WindowsのVPN画面へ案内する。
- どの画面にも出ていないウィンドウを一覧に出し、選んだ 1 つを今の画面へ戻す。
  動かせないものも理由つきで一覧に残す。
- ゲーム中に Shift の連打・長押しで出る確認画面を止める。
  固定キー機能そのものは無効にしない。止めるのはショートカット起動だけ。
- 電源接続時と電池使用時の電源モードを選ぶ。選べない値がある PC では、
  そう表示して何も変更しない。
- マウスポインターの加速をモードごとに入り切りする。ゲームが独自にマウスを
  読み取っている場合は届かない、と画面に書く。
- コントラストテーマを 30 秒だけ試し、時間切れか明示操作で開始前の全フィールドへ
  戻す。開始時点で既に有効なら変更せず、途中の外部変更も上書きしない。
- 最大化しているときだけタスクバーを隠す。既定は切。手で変えられたら手を引く。
- いまどのモードかを、タスクバーのすぐ上の細い帯で示す。Windows の設定は変更しない。
- 画面の角にマウスを置くとモード画面を開く。既定は 4 隅とも「何もしない」。
- 名前から選ぶ配色シーン。既にある 3 項目を束ねるだけで、新しい変更手段は増やさない。
- 適用した設定が今も残っているかの照合。原因は書かず、確認できないものは分けて出す。
- 時刻によるライト／ダークの自動切り替え。
- 一時ファイルの削除（7 日より古いファイルのみ、二段階確認つき）。
- 設定の書き出しと取り込み。データのみで、コードは含まない。

### 決めたこと

- 管理者権限で動かさない。昇格ヘルパーは同梱しない。
- Microsoft Defender、Windows ファイアウォール、Windows Update を止める機能は入れない。
- PowerShell の文字列を実行しない。画面から渡せるのは登録済みの Action ID と
  検証済みパラメーターだけ。
- PowerToys に同等機能があるものは作り直さず、導入と設定を案内する。

### 既知の制限

- 検証は Windows 11 build 26200 の 1 台のみ。他ビルドでは推測せず復旧モードに入る。
- インストーラーに署名がないため、SmartScreen が警告を出す。
- 自動更新の仕組みはまだない。
- アンインストールしてもデータベースは残る。変更を元へ戻すための記録が入っているため。
