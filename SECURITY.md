# 脆弱性の報告

見つけた場合は、Issue ではなく GitHub の
[Private vulnerability reporting](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)
から報告してください。公開の Issue に書くと、修正版が出る前に手口が広まります。

返信は数日かかることがあります。個人で開発しているためです。

## このアプリで特に見たい種類の問題

このアプリは利用者の Windows 設定を書き換え、それを元に戻す責任を持ちます。
以下は「動作不良」ではなく脆弱性として扱います。

- **ロールバックが元の状態へ戻らない経路。** 特に、元が「値なし」だった項目が
  既定値で復元されてしまう、型が変わる、キーの有無が変わる、といったもの。
- **第三者の変更を上書きしてしまう経路。** 適用後に他のアプリが同じ値を変えた場合、
  ロールバックは止まらなければなりません。
- **画面から渡した文字列が、コマンド行・レジストリパス・ファイルパスへ検証なしに届く経路。**
- **登録されていない Action ID やパラメーターが実行されてしまう経路。**
- **ログ・エラーメッセージ・エクスポートしたファイルに個人情報が出る経路。**
  ウィンドウタイトルはファイル名やメールの件名を含むため、特に注意しています。
- **`ActionKind::Guided`（表示専用）の項目が、実際には OS を変更してしまう経路。**

## 対象外

- 検証していない Windows ビルドで動かないこと。これは意図した挙動で、
  知らないビルドでは推測せず復旧モードに入ります。
- 表示専用の 42 項目が変更できないこと。これも意図した挙動です。
  理由は [README](README.md) に書いています。
- インストーラーに署名がないため SmartScreen が警告すること。
  署名の準備は別途進めています。

## 対応方針

報告を受けたら、まず再現します。再現できたものは、直したうえで
何が起きうるかを `docs/` に記録します。伏せません。
